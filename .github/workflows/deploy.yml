name: Deploy to Pages

on:
  push:
    branches: ["master"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write
  actions: read

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: pnpm/action-setup@master
        with:
          version: 9
      - uses: actions/setup-node@master
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile --allow-build=@fortawesome/fontawesome-common-types@6.4.0,@fortawesome/free-solid-svg-icons@6.4.0,@sveltejs/kit@1.15.5,esbuild@0.17.16,node-sass@8.0.0,svelte-preprocess@5.0.3
      - run: pnpm run build
      - run: ls -la build

      - uses: actions/upload-pages-artifact@master
        with:
          path: ./build

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/configure-pages@master
      - id: deployment
        uses: actions/deploy-pages@master
